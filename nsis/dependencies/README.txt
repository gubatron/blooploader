These are zip files with NSIS extensions that our NSIS script needs.

Things like Downloading, or Unzipping are not included by default in some versions of NSIS.

Basically unzip them and copy the DLLS to
<NSIS_INSTALL_FOLDER>\Plugins

